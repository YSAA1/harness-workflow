# Architecture Layers

This file is the authoritative layer map only after the Harness Charter and Coverage Matrix approve it.

## Source roots

{% for root in source_roots %}- `{{ root }}`
{% endfor %}

## Layer order

Dependency direction: lower-level layers may not import higher-level layers.

| Layer | May import | Notes |
| --- | --- | --- |
{% for layer in layers %}| `{{ layer.name }}` | {{ layer.allowed_imports | join(", ") if layer.allowed_imports else "none" }} | {{ layer.notes | default("") }} |
{% endfor %}

## Exceptions

{% if exceptions %}{% for exception in exceptions %}- {{ exception }}
{% endfor %}{% else %}None approved.
{% endif %}

## Remediation

If a violation is reported, move the dependency behind an approved lower-level interface, provider, adapter, or service boundary. Error messages should include file, import target, layer relation, and this file path.
