# Commit Convention

Use concise commit messages that describe the approved slice.

Recommended trailers when useful:

- `Spec:` link to the approved spec or issue
- `Plan:` link to the executable plan or active slice
- `Evidence:` command or artifact proving the claim

Do not require trailers for tiny mechanical edits unless the project already enforces them.