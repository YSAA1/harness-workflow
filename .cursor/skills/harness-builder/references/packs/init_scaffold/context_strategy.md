# Context Strategy

Static context lives in the repo and changes slowly: `AGENTS.md`, layer docs, durable rules, guides, and verification constraints.

Dynamic context is probed fresh: git status/log, diagnostics/lint, CI status, selected recovery surface, boundary test status, documentation drift, and runtime signals when already exposed.

The pack can provide checklists, but builder evidence collection remains authoritative.
