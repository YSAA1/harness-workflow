# Optional instruction outline
按文件用途选择：项目入口、关键合同、环境/运行入口、最窄验证、真正非显然的陷阱。
全局文件保留稳定个人偏好；项目根保留导航；领域细节放适用子目录。状态和历史放已有恢复面。
Codex 全局为 $CODEX_HOME/AGENTS.md 或 AGENTS.override.md（默认 ~/.codex）；Claude Code 为其 CLAUDE.md 配置；Cursor 修改 canonical rules 后同步适配面。不要跨运行时套用发现路径。
模板是菜单，不要求填满或复制通用最佳实践。
