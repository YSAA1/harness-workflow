# Research Log - skills.sh 收录缺口

> Track: work_index #027 | 控制模式: auto | 预算: 3 轮 × 每轮 1 次有成本实验 | 状态: inconclusive

## 背景与范围（固定，全程不变）

- 一句话目标（逐字引用）：授权语=「合入后立即拿一个真实小问题首跑」（任务 026 计划 W8）；研究问题为本会话实测遗留观察：「skills.sh 的仓库页为什么只显示 YSAA1/harness-workflow 的 13/17 个技能」（自立校准目标，推断部分已标注）。
- 范围边界（问什么 / 不问什么）：问该仓库页的收录机制；不问平台级排名算法、不修改任何用户级配置。
- 完成判据（answered 定义，可验证）：以一手来源明文或判别性观察确定该页技能收录规则；或以用尽的廉价探针诚实报告不可裁决及原因。
- 已知事实与来源（立项摸底产出）：页面 13 技能/37 安装、无快照元数据（skills.sh/YSAA1/harness-workflow，2026-09-23 两次抓取一致）；CLI 存在安装遥测、未文档化收录规则（github.com/vercel-labs/skills README）；缺失四技能=sweep/autoresearch/research/handoff。
- 候选解释（后续轮次要区分的竞争假设）：(a) 仅列出 ≥1 次安装事件的技能；(b) 索引快照滞后（爬取早于四技能合入）；攻击轮补充：(c) 安装事件驱动摄入（页面在遥测事件时点摄入当次树内技能）；(d) 同名全局键碰撞；(e) frontmatter 解析失败排除。

## 第 1 轮

- 假设（可证伪命题）：仓库页只列出有 ≥1 次安装遥测事件的技能（a）。
- 预测（足以区分本轮竞争解释；不扩大、不偷换用户范围）：若 a 成立——缺失四技能计数为 0；目录不出现 0 计数条目；rdp（同期合入）上榜与其 ≥1 安装兼容。
- 验证方式：抓取仓库页计数分布；抓目录首页看是否存在 0 计数条目；读 CLI 仓库 README 找收录规则明文。
- 成功判据：一手明文规定收录规则，或构造出能区分 a/b 的观察。
- 批准（auto）。
- 干活·观察：①仓库页 13 技能，12 个各 3 次、remove-deadcode-py 1 次，合计 37，无快照日期（来源：skills.sh/YSAA1/harness-workflow，2026-09-23 两次）；②目录首页为按安装数降序的 top-N 榜（最低可见约 172K），未见 0 计数条目（来源：skills.sh 首页）——**该观察经攻击降级为背景：榜单截断本身必然无 0 条目，对收录规则零证明力**；③CLI README 确认安装遥测存在（公开仓库发送 repo+skill 标识），未文档化收录规则（来源：github.com/vercel-labs/skills）。本地实验：`git log --follow` 时间戳——remove-deadcode-py 首次合入 **2026-09-16 14:24**，sweep 09-21 15:31，autoresearch 09-21 19:42，research/handoff 09-23 01:46（本仓 git，T5 证据）。站内搜索探针不可用：/search 为 JS 壳无服务端渲染结果。
- 审核·对抗裁决（independent，新生上下文红队）：Critical×3——C1 原「全量安装后复查」检验无判别力（所有假设预测相同）；C2 字面 b 被自有数据质疑；C3 「缺失四技能 0 安装」为未验证承重前提。Important×3——I1 假设空间补 (c)(d)(e)；I2 观察②零证明力；I3 廉价探针未用尽前不宜宣告 inconclusive。处置：C1 采纳（判别检验改设计，见终态）；C2 部分采纳但被时间戳探针修正——rdp 实为 09-16 合入（攻击者收到的「同期合入」前提有误），b-窄窗口为 09-16→09-21 共 5 天+其后 2 天无再摄入，**并不像攻击推演的那样不可行**；C3 采纳（0 安装前提显式标注为未验证）；I1/I2 采纳；I3 采纳（已补跑时间戳与搜索两探针）。
- 蒸馏·结论：部分证实——(a) 未被证实为明文规则；(b) 字面版存活但需窄窗口；**领先解释更新为 (c) 安装事件驱动摄入**：3 次历史 CLI 安装解释 12×3；rdp 仅最近一次安装（09-16 后、09-21 15:31 前）晚于其合入故计数 1；四技能合入（09-21 15:31–09-23 01:46）晚于最后一次安装事件故从未摄入。该解释统一覆盖全部计数观察，但仍为推断，无平台文档佐证。
- 蒸馏·lessons：新增一条（见 .harness/lessons.md 环境与工具：新技能发布后需一次 CLI 安装触发收录）。

### 重开提案（须对抗审查后决定）

- 本轮排除了什么（附证据）：排除了「页面快照早于 09-16」（rdp 已上榜且计数 1，git 09-16 14:24）；排除了观察②作为证据（榜单截断必然无 0 条目）。
- 尚未解决的剩余解释：(a)≥1 安装过滤、(b)-窄窗（09-16→09-21 爬取+2 天无再摄入）、(c) 事件驱动摄入、(d) 同名碰撞、(e) 解析失败。
- 下一轮如何区分它们：判别性安装检验（需用户授权，见终态）可一次性分离——装后四技能齐现且计数 1 ⇒ (a)/(c) 族且排除纯 b；不装数日后自现 ⇒ b；装后仍缺 ⇒ (d)/(e)。0 安装仓库页探针与站内搜索探针已试不可用。
- 裁决：**不重开**——剩余判别检验的唯一执行路径（真实 CLI 安装）触及用户级配置（AGENTS.md Protected Paths），须用户授权；在授权前开新轮无信息增益，诚实停止。

## 第 2 轮（用户授权判别性安装检验后重入）

- 假设（可证伪命题）：安装事件即时驱动页面更新（第 1 轮领先解释 c 的即时版）；隐含前提=页面计数实时。
- 预测：成功安装 17 技能（含四缺失）后数分钟内页面变 17/17、四技能计数 1、12 个老技能计数 3→4。
- 验证方式：隔离项目级真实安装（/tmp/skills-install-test，`npx skills@latest add YSAA1/harness-workflow -s '*' -y`，遥测通道正常）→ 间隔复查页面（渲染抓取，含 query 参数穿透抓取方缓存）。
- 成功判据：安装后页面出现任何可观察变化（计数或列表）。
- 批准（auto；安装动作经用户显式授权，绕开 Protected Paths 暂停点）。
- 干活·观察：①安装成功——17/17 落盘、CLI 报 universal+symlinked 分发、落盘 SKILL.md 实测为 0.12.0 新版（来源：/tmp 安装目录实测，已清理）；②安装后 ≥5 分钟两次复查：页面 13/37 完全不变（tdd 仍 3、remove-deadcode-py 仍 1、四缺失仍缺；来源：skills.sh 渲染抓取 ×2，第二次带缓存穿透参数）；③原始 HTML 为 JS 壳，curl 直读不可监控（0 命中）。
- 审核·对抗裁决（self，降级自审显式记录——本轮结论为机械二值主源复核，未派独立子代理；具体攻击与反例尝试如下）：攻击 1「抓取方 15 分钟缓存造成假不变」——反例尝试：第二次复查改用新 query 参数穿透且距安装 >5 分钟，仍不变；攻击 2「skills.sh 自家 CDN 边缘缓存」——未排除，标注为残余混杂（若成立则结论顺延到缓存过期后）；攻击 3「遥测未发出（代理拦截）」——安装经代理成功完成、CLI 无遥测失败报错，无法直接验证发送，标注为残余混杂。
- 蒸馏·结论：证伪（对本轮假设）——即时版事件驱动与实时计数被驳；附带证伪 (e) 解析失败（CLI 成功解析并安装全部 17 个）。页面为**批量/爬取节奏的快照**。修正后的判别性观察：盯住页面下一次任何变动（预期老技能 3→4）——四缺失**同刻出现** ⇒ 摄入与安装事件绑定（批粒度 a/c 族，b 纯爬虫被驳）；**计数变而四仍缺** ⇒ 树爬取+过滤（但须解释独名 autoresearch 仍缺，(d) 存疑）；**长期全不变** ⇒ 更新节奏 ≥ 天级。
- 蒸馏·lessons：更新 skills.sh 条目（页面批量快照非实时；安装后隔日复查收录）。

### 重开提案（第 2 轮）

- 本轮排除了什么（附证据）：即时事件驱动更新、实时计数（安装后 ≥5 分钟页面零变化，含缓存穿透复查）；(e) 解析失败（CLI 实装 17/17）。
- 尚未解决的剩余解释：批粒度事件驱动摄入（a/c 批版）、纯周期爬虫（b）、页面侧 CDN 缓存延迟。
- 下一轮如何区分它们：等待页面下一次变动再读上表三分支；无主动可执行探针（0 计数仓库页与站内搜索探针已证不可用，安装探针已用尽其即时判别力）。
- 裁决：**不重开**——判别依赖平台侧批量更新（节奏未知），等待期无信息增益；诚实停在 inconclusive（较第 1 轮已收窄三支）。

## 终态（停止时填写；经第 2 轮重入后更新）

- 停止状态：inconclusive（第 2 轮后收窄：即时更新、实时计数、解析失败三支已被驳；残留批粒度事件摄入 / 周期爬虫 / CDN 缓存延迟三支待平台侧下次变动区分）
- 结论（结论先行＋逐条证据链接；对照完成判据逐条覆盖情况）：**skills.sh 仓库页是批量/爬取节奏的快照，不是实时页面**——用户授权下完成一次真实 CLI 全量安装（17/17 实装、0.12.0 内容核验），安装后 ≥5 分钟页面计数与列表零变化（tdd 仍 3、rdp 仍 1、四缺失仍缺，含缓存穿透复查）。结合第 1 轮计数分布与 git 时间戳（12×3 对应 3 次历史安装、rdp 1 对应唯一晚于其 09-16 合入的安装、四技能 09-21~09-23 合入晚于最后一次安装事件），最可能机制仍为**摄入与安装遥测事件绑定、批量生效**；纯周期爬虫未排除。判别性复查点（被动等待）：页面下次任何变动（预期老技能 3→4）时四缺失是否同刻出现——同刻 ⇒ 事件绑定确认；计数变而仍缺 ⇒ 爬取+过滤（独名 autoresearch 仍缺则存疑）；长期不变 ⇒ 节奏 ≥ 天级。完成判据覆盖：一手明文未达成（平台未文档化）；廉价探针用尽后诚实报告已达成（0 计数仓库页/站内搜索/即时安装三探针均已执行或证不可用）。残余混杂：页面侧 CDN 缓存、遥测发送未直接核验（均已在第 2 轮审核段标注）。
- 未能得出的结论：平台级收录规则（单仓库 n=1 限域）；(a) 过滤与 (c) 事件摄入的批粒度细分；「缺失四技能安装数为 0」在第 2 轮安装后已不成立（本轮安装已含四技能，页面待批量更新验证）。

## 遗留

判别性安装检验待用户授权执行（预计一次性裁决 a/b/c/d/e）；若确认事件驱动摄入，可在 README 或 install.md 补一句「新技能收录需一次 CLI 安装触发」的操作提示。
