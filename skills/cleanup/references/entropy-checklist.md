# Cleanup scope
仅清理本任务创建、已无用途的 scratch。未跟踪、未被链接或名称含 temp 均不足以证明可删除。
代码遗留同理：死代码、未用导入/依赖、孤儿脚本要有引用证据才删，删后跑针对性检查确认行为不变。
保留实验验收、复现、来源和恢复工件；未知归属先保留并说明。
修复本次影响的命令、接口或状态文档。无漂移可零修改；阻塞交接应准确保留失败与下一步，不等待 ready。
超出本任务的历史清理作为后续事项，不自动执行；用户显式要求全仓 Python 死代码清理时转 `remove-deadcode-py`，全仓对账/盘点转 `sweep`。
