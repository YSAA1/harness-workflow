{% set lang_norm = (target_language | default(user_language | default('zh')) | string | lower) %}
{% set zh = lang_norm[:2] == 'zh' or '中文' in lang_norm or 'chinese' in lang_norm %}
# {{ '工作索引（Work Index）' if zh else 'Work Index' }}

{{ '任务注册表（T3，Required）。`AGENTS.md` 只指向本文件。' if zh else 'Task registry (T3, Required). `AGENTS.md` points here only.' }}

| ID | {{ '标题' if zh else 'Title' }} | Status | Primary artifact | {{ '最近验证' if zh else 'Last verified' }} |
| --- | --- | --- | --- | --- |
| example | Example | complete | `docs/plans/YYYY-MM-DD--example-plan.md` | YYYY-MM-DD |

{{ 'Status values' if zh else 'Status' }}: `active`, `blocked`, `complete`, `abandoned` — {{ '除非显式声明并行，同一时刻只能有一个 `active`。' if zh else 'only one `active` unless parallelism is declared.' }}
