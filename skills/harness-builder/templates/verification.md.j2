# Verification

Target command: `{{verification_command}}`
Use its actual shell, cwd and environment. Add a wrapper only when it solves a real need.
Record required claim → evidence → pass/fail/unknown. Reuse applicable outputs; rerun affected checks after relevant changes.
Do not report behavior ready from file existence, logs alone or structural checks.
