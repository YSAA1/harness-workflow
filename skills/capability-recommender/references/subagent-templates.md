# Subagents
仅委派边界清晰、可独立推进且节省时间或提升审查质量的工作。输入给目标、相关文件/证据、权限、预期结果；独立审查默认只读。
模型沿用用户或宿主配置，不写死 sonnet 或某个家族。文件数不是启用子代理的阈值，普通任务无需多代理。
缺少子代理工具时可继续可独立完成的任务，并如实说明审查独立性。项目明确要求独立审阅的合同仍须满足。
持久配置仅在用户需要复用自动化时创建，不为一次委派写入项目配置。
