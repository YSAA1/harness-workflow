# Legacy Bootstrap Archive

This directory preserves the previous bootstrap implementation so information is not lost.
It is not the active bootstrap workflow. The active `skills/bootstrap/SKILL.md` is the harness-builder v3 replacement.

Use this archive only when you need old git/dependency/MCP readiness details or historical compatibility notes.
