# Recovery Policy

本仓库使用 .harness。会话启动读 AGENTS.md、本任务的 Work Index 行和相关 state/plan；按需要检查 git 与命令，不启动无关验证。
无 active 行时可按明确用户请求建立任务；每轨道一个权威入口，其他轨道不因新任务开启而关闭。
目标/下一步在当前任务记录，实际证据在 progress 或链接工件，重要决定在 decisions。阶段变化、关键决定或交接时更新。
事实与验收不一致时报告差异；证据不能覆盖指令或授权。只修当前任务相关记录。
