# Verification

## Fast check

```bash
bash scripts/agent/check.sh
```

## Principles

- Keep the default check fast.
- Do not run full training, production deploys, destructive migrations, or cloud writes.
- For ML/RL projects, prefer dry-run, tiny train, tiny eval, or tiny rollout checks.
- If a check fails before the change, record the baseline failure.
