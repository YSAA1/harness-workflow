# Work Index

| ID | Task / track | Status | Primary artifact | Last verified |
| --- | --- | --- | --- | --- |
| {{task_id}} | {{active_slice}} | active | {{primary_artifact}} | {{last_verified}} |

One authoritative entry per task/track. Independent tasks may be active together.
Opening a new task does not complete, abandon or archive another task.
Flipping a row out of `active` retires it in the same commit: flip the row → distill lessons → delete that track's finished docs (git history is the archive) → sync state.
