# Persistent working context
Draw on the principle of durably recording goals, important findings, and next steps, reusing the project's existing backend.
Do not write to disk per tool invocation; do not create task_plan.md/findings.md-style root-directory files by default; do not reopen completed tasks because old records were read.
Update only the information recovery needs — at phase changes, key decisions, and hand-offs.
