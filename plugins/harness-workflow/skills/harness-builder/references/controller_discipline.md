# Controller discipline
只读推荐不授权安装或产品执行；明确要求修复/优化时完成已授权范围内的常规编辑和验证，不重复要求批准精确 patch。
跨 helper 保留授权和上下文。简单修改可以直接执行；厚任务使用对应 helper，独立调查可并行。
只处理真实 gap；没有 gap 可零修改结束。用户同时授权工作台修复及产品任务时连续完成，不能强制停在路由建议。
不引入外部研究治理、默认 branch guard 或全局配置。复杂完成声明用 review（verify 为别名）。
