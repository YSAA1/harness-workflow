#!/usr/bin/env python3
# Optional illustrative template: adapt and test for the target runtime before use.
# Not a security boundary; never install by default.
from __future__ import annotations

import json

command = json.loads({{ verification_command | tojson | tojson }})
print(f"Reminder: before claiming done, run or explain {command!r} and record evidence.")
