// Add to ESLint only after architecture lint enforcement is approved.
// Prefer warning/baseline behavior for existing repos.
export default [
  {
    rules: {
      "no-restricted-imports": ["error", {{ restricted_imports_json }}]
    }
  }
];
