{% set lang_norm = (target_language | default(user_language | default('zh')) | string | lower) %}
{% set zh = lang_norm[:2] == 'zh' or '中文' in lang_norm or 'chinese' in lang_norm %}
# {{ 'Harness 状态（Harness State）' if zh else 'Harness State' }}

{{ '本文件是热恢复索引，不是追加式报告。旧进展应压缩为当前状态、决策、风险和证据链接。' if zh else 'This file is a hot recovery index, not an append-only report. Roll old progress into current state, decisions, risks, and evidence links.' }}

## {{ '项目阶段（Project phase）' if zh else 'Project phase' }}

{{ project_phase }}

## {{ '风险级别（Risk level）' if zh else 'Risk level' }}

{{ risk_level }}

## {{ 'Harness 构思摘要（Harness brainstorming summary）' if zh else 'Harness brainstorming summary' }}

{{ brainstorming_summary }}

## {{ 'Harness 推荐契约（Harness Recommendation Contract）' if zh else 'Harness Recommendation Contract' }}

- {{ '目标（Objective）' if zh else 'Objective' }}: {{ harness_objective }}
- {{ '验收标准（Acceptance criteria）' if zh else 'Acceptance criteria' }}: {{ acceptance_criteria }}
- {{ '验证路径（Verification path）' if zh else 'Verification path' }}: {{ verification_path }}
- {{ '证据位置（Evidence location）' if zh else 'Evidence location' }}: {{ evidence_location }}
- {{ '选定 recovery surface' if zh else 'Selected recovery surface' }}: {{ recovery_surface }}

## {{ '真相源优先级（Source-of-truth priority）' if zh else 'Source-of-truth priority' }}

{{ source_of_truth_priority }}

## {{ '当前工作（Active work）' if zh else 'Active work' }}

- {{ '当前 active task' if zh else 'Current active task' }}: {{ current_active_task if current_active_task is defined else ('未记录。' if zh else 'None recorded.') }}
- {{ '证据位置' if zh else 'Evidence location' }}: {{ evidence_location }}
- {{ '风险' if zh else 'Risks' }}: {{ current_risks if current_risks is defined else ('未记录。' if zh else 'None recorded.') }}
{{ '下一步（checkbox 工作项）' if zh else 'Next actions (checkbox work items)' }}:

{{ next_actions if next_actions is defined else ('- [ ] 未记录。' if zh else '- [ ] None recorded.') }}

{% if include_recommendation_matrix %}
## {{ '推荐矩阵（Harness Recommendation Matrix）' if zh else 'Harness Recommendation Matrix' }}

| {{ '推荐区域（Area）' if zh else 'Area' }} | {{ '状态（Status）' if zh else 'Status' }} |
| --- | --- |
| {{ 'Agent 入口和项目地图' if zh else 'Agent entry and project map' }} | {{ recommendation_agent_entry }} |
| {{ '静态文档和稳定规则' if zh else 'Static docs and durable rules' }} | {{ recommendation_static_docs }} |
| Recovery surface | {{ recommendation_recovery_surface }} |
| {{ '验证入口' if zh else 'Verification entry' }} | {{ recommendation_verification_entry }} |
| {{ '架构边界' if zh else 'Architecture boundaries' }} | {{ recommendation_architecture_boundaries }} |
| Anti-entropy | {{ recommendation_anti_entropy }} |
| Skill fit | {{ recommendation_skill_fit }} |
| Hook fit | {{ recommendation_hook_fit }} |
| MCP fit | {{ recommendation_mcp_fit }} |
| Subagent fit | {{ recommendation_subagent_fit }} |
| {{ '外部研究适配' if zh else 'External research fit' }} | {{ recommendation_external_research_fit }} |
| {{ '动态上下文' if zh else 'Dynamic context' }} | {{ recommendation_dynamic_context }} |
{% endif %}

{% if recommendation_summary %}
## {{ '推荐摘要（Recommendation summary）' if zh else 'Recommendation summary' }}

{{ recommendation_summary | default('未记录推荐摘要。' if zh else 'No recommendation summary recorded.') }}
{% endif %}

{% if orchestration_mode %}
## {{ '编排模式（Orchestration mode）' if zh else 'Orchestration mode' }}

{{ orchestration_mode }}

{{ '原因' if zh else 'Reason' }}:

{{ orchestration_reason }}
{% endif %}

## {{ '待用户决策（Open user decisions）' if zh else 'Open user decisions' }}

{{ open_user_decisions }}

## {{ '最近一次已知可用验证（Last known good verification）' if zh else 'Last known good verification' }}

{{ '未记录。' if zh else 'None recorded.' }}

## {{ '已知失败检查（Known broken checks）' if zh else 'Known broken checks' }}

{{ '未记录。' if zh else 'None recorded.' }}

## {{ '近期重要决策（Important recent decisions）' if zh else 'Important recent decisions' }}

{{ '见 `.harness/decisions.md`。' if zh else 'See `.harness/decisions.md`.' }}

## {{ '已知缺口（Known gaps）' if zh else 'Known gaps' }}

{{ known_gaps }}

## {{ '建议下一步（Next recommended step）' if zh else 'Next recommended step' }}

{{ next_steps }}
