# AGENTS.md

## Project overview

{{ project_overview }}

## Project map

{{ project_map }}

## Quick start

{{ quick_start }}

## Project iron laws

These are non-negotiable project rules. Do not violate them to make a task appear complete.

{{ project_iron_laws }}

## Recovery surface

Selected recovery surface: {{ recovery_surface }}

Read next:

{{ recovery_read_order }}

Dynamic task state does not live in `AGENTS.md`.

## Required reading by task type

{{ required_reading }}

## Source-of-truth priority

{{ source_of_truth_priority }}

Do not treat old state as current just because it exists. If sources conflict, follow the priority above and current git evidence.

## Verification

Fast check:

```bash
{{ fast_check_command }}
```

Do not claim success if this was not run. If it fails, report the failure honestly and do not hide or bypass it.

## Scope control

Work on one task or one feature at a time. Do not perform unrelated refactors unless explicitly requested.

## Protected areas

Do not modify unless explicitly requested:

{{ protected_paths }}

## Definition of Done

A task is done only when:

- the change is minimal and task-scoped;
- relevant verification was run or the failure is reported;
- selected recovery surface is updated for multi-session work;
- remaining risks are listed.
