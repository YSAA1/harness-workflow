{% set lang_norm = (target_language | default(user_language | default('zh')) | string | lower) %}
{% set zh = lang_norm[:2] == 'zh' or '中文' in lang_norm or 'chinese' in lang_norm %}
# {{ '会话交接（Session Handoff）' if zh else 'Session Handoff' }}

{{ '当会话结束但工作未完成时使用这个文件。' if zh else 'Use this file when a session ends before the work is complete.' }}

## {{ '最近工作上下文（Last working context）' if zh else 'Last working context' }}

{{ '未记录。' if zh else 'None recorded.' }}

## {{ '已触及文件（Files touched）' if zh else 'Files touched' }}

{{ '未记录。' if zh else 'None recorded.' }}

## {{ '已运行命令（Commands run）' if zh else 'Commands run' }}

{{ '未记录。' if zh else 'None recorded.' }}

## {{ '当前阻塞项（Current blockers）' if zh else 'Current blockers' }}

{{ '未记录。' if zh else 'None recorded.' }}

## {{ '建议下一步（Suggested next step）' if zh else 'Suggested next step' }}

{{ '未记录。' if zh else 'None recorded.' }}
