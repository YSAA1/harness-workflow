# Skills
只在可复用工作流或领域资料能补足实际缺口时推荐技能。检查已有技能和目标运行时的发现机制，避免同职责入口互抢。
Codex 的 agents/openai.yaml 可控制 allow_implicit_invocation；Claude Code 的 disable-model-invocation 等字段不是跨平台通用配置。以目标运行时当前文档为准，保留用户已选择的调用策略。
优先清晰触发边界、可检查资源和必要工具。热门数量不证明质量；不要因为普通任务“可能有 skill”就先去搜索。
