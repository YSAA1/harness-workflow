# Hooks
仅在重复、明确且可机械验证的行为值得自动执行时建议 hook。文件存在或采用某种语言本身不意味着需要 hook。
复用已有 formatter/CI，避免每次编辑触发全量 lint/typecheck 或自动修改无关文件。明确匹配范围、运行时、失败行为、停用方式和最小探针。
示例 hook 不是安全边界。锁文件可由包管理器正常更新；凭据配置遵循宿主权限和用户授权，不机械封锁所有配置写入。
不默认安装，跨 runtime 核对事件 schema。
