# Instruction updates
允许新增、删减、合并和迁移；每项需服务用户目标。持久入口只保留稳定合同、非显然事实及准确命令。
保留既有用户改动，修改 canonical source 后同步镜像。审计请求只读；明确编辑授权不重复求批；超出范围或关键合同改变需澄清。
验证引用路径、命令语义、diff 和生成一致性。没有必要时不运行安装、部署或完整训练来“验证文档”。
报告实际变更及未验证限制。零修改也是有效结果。
